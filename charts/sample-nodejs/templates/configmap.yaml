apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sample-nodejs.fullname" . }}
  labels:
    {{- include "sample-nodejs.labels" . | nindent 4 }}
data:
  APP_ENV: {{ default "development" .Values.config.appEnv | quote }}
  LOG_LEVEL: {{ default "info" .Values.config.logLevel | quote }}